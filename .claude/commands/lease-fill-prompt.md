Read `/Users/rayansh/Documents/amrikahousing/src/lib/fill-lease.ts` and print the following exactly as they appear in the file, each formatted as a code block with a clear label:

1. "Token list:" — the `TOKEN_LIST` constant
2. "Replacement map:" — the `buildReplacementMap` function
3. "Generate lease logic:" — the `generateLease` function

Then read `/Users/rayansh/Documents/amrikahousing/src/app/api/leases/fill-template/route.ts` and print:

4. "Manager resolution:" — the block of code that looks up `assignedManager` and `managerUser` (the property manager fallback logic)
